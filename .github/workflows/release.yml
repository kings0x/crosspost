on:
  push:
    branches:
      - "main"

jobs:
  validate:
    name: some
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: some
        uses: action/checkout@4
