global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_config:
  - job_name: "seidar-api" # a label to identify your app in grafana
    static_configs:
      - targets: ["host.docker.internal:9090"] # where your Go app exposes metrics
